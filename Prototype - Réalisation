CREATE DATABASE blog_db;
USE blog_db;


CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100)
);


CREATE TABLE posts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255),
    content TEXT,
    user_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id) REFERENCES users(id)
);


CREATE TABLE comments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    content TEXT,
    user_id INT,
    post_id INT,

    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (post_id) REFERENCES posts(id)
);

INSERT INTO users (name, email) VALUES
('Ayoub', 'ayoub@gmail.com'),
('Sara', 'sara@gmail.com'),
('Yassine', 'yassine@gmail.com');

INSERT INTO posts (title, content, user_id) VALUES
('My First Post', 'Welcome to my blog', 1),
('Learning SQL', 'SQL is easy', 2),
('Web Development', 'HTML CSS JS PHP', 1);

INSERT INTO comments (content, user_id, post_id) VALUES
('Great post!', 2, 1),
('Very helpful', 3, 2),
('Nice article', 1, 2);

SELECT posts.title, users.name
FROM posts
INNER JOIN users
ON posts.user_id = users.id;

SELECT * FROM posts
WHERE title = 'SQL Course';

SELECT COUNT(*) AS total_posts
FROM posts;

SELECT posts.title, users.name
FROM posts
INNER JOIN users
ON posts.user_id = users.id
WHERE users.name='Ayoub';

UPDATE users
SET name = 'Mohamed'
WHERE id = 1;

